%h1
  Mes matières

%table.table
  %tr
    %th Nom
    %th Début des interventions
    %th Fin des interventions
    %th Modifier
    %th Supprimer
    %th Gestion des étudiants

  - @disciplines.each do |d|
    %tr
      %td= d.name
      %td= d.beginsOn
      %td= d.endsOn
      %td= link_to 'Modifier', edit_discipline_path(d)
      %td= link_to 'Supprimer', discipline_path(d), method: :delete, data: { confirm: 'êtes vous sûr ?' }
      %td= link_to 'gérer les étudiants', discipline_edit_students_path(d)

= link_to 'Ajouter une matière', new_discipline_path
